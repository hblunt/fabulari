// client/src/app/features/admin/banned-accounts-page.ts
// Stage 0 placeholder for BannedAccountsPage (wireframe 14): tombstone
// records of permanently banned accounts. Built in Stage 4 (feat/admin).

import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-banned-accounts-page',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: `
    <section class="p-8">
      <h1 class="text-2xl font-semibold">Banned accounts</h1>
      <p class="mt-2 text-muted-foreground">BannedAccountsPage — built in Stage 4 (feat/admin).</p>
    </section>
  `,
})
export class BannedAccountsPage {}
