{
  "name": "fabulari-server",
  "version": "1.0.0",
  "private": true,
  "description": "Express API for the Fabulari chat application (Phase 1)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --ignore data/ --ignore seed/ server.js",
    "reset": "node scripts/reset.js",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
