# server/.env.example
# Copy to server/.env if you need a non-default Mongo URL (Atlas, Docker, etc).
# server/.env is gitignored. Without a file, the default local URI is used.

MONGODB_URI=mongodb://127.0.0.1:27017/fabulari
